apiVersion: v1
kind: Secret
metadata:
  name: capstone-backend-secrets
  labels:
    app: capstone-backend
    circleci.com/component-name: capstone-backend
type: Opaque
data:
  # Base64 encoded values - replace with your actual encoded secrets
  # Example: echo -n "your-secret-value" | base64
  database-url: ""  # Add your base64 encoded database URL
  jwt-secret: ""    # Add your base64 encoded JWT secret
  email-password: "" # Add your base64 encoded email password
  # Add other sensitive configuration here
